function click (posX, posY, Tam){ //funcion con parametros
  //volver a la pantalla de inicio por ej
  //función que se utiliza en Pasar Pantalla
  return mouseX>posX && mouseX<posX + Tam/2
  mouseY>posX&& mouseY<posY + Tam;
}

//no tocar nada
